{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": [
        "**/.env.*local"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "^build",
                "^generate"
            ],
            "outputs": [
                ".next/**",
                "!.next/cache/**",
                "dist/**"
            ]
        },
        "generate": {
            "cache": false
        },
        "lint": {},
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}